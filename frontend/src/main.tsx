import React from "react";
import ReactDOM from "react-dom/client";
import ContactCallerApp from "./contact_caller_app";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ContactCallerApp />
  </React.StrictMode>
);
